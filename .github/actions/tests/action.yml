name: 'Run tests'
description: 'Run molecule against the code'
runs:
  using: "composite"
  steps:
    - name: Run molecule
      id: Molecule
      run: MOLECULE_NO_LOG="false" molecule converge
      shell: bash
