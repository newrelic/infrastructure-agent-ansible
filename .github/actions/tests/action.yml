name: 'Run tests'
description: 'Run molecule against the code'
runs:
  using: "composite"
  steps:
    - name: Run molecule
      id: Molecule
      run: ansible-lint lintables tasks
      shell: bash
