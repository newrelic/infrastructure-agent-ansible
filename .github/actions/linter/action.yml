name: 'Run linter'
description: 'Run linter against the code'
runs:
  using: "composite"
  steps:
    - name: Run linter
      id: linter
      run: ansible-lint tasks/* -x 106,204 -c .yamllint
      shell: bash
